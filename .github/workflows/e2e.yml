name: Majang Mejeng V9 E2E

on:
  push:
    branches: [main, v9-rebuild]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  e2e:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.15.0
          run_install: false
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm
          cache-dependency-path: pnpm-lock.yaml
      - name: Install dependencies
        run: pnpm install --no-frozen-lockfile
      - name: Typecheck application
        run: pnpm --dir artifacts/majang-mejeng run typecheck
      - name: Production build
        run: PORT=4173 BASE_PATH=/MAJANGMEJENG/ NODE_ENV=production pnpm --dir artifacts/majang-mejeng run build
      - name: Install Playwright Chromium
        run: pnpm exec playwright install --with-deps chromium
      - name: Run V9 browser journeys
        run: pnpm exec playwright test tests/e2e
      - name: Upload Playwright report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: majang-mejeng-v9-playwright-report
          path: artifacts/majang-mejeng/playwright-report
          if-no-files-found: ignore
